/**
 * Application configuration.
 * Only the API base URL lives here. No other file should hardcode a URL.
 */
window.APP_CONFIG = {
  API_BASE_URL: "http://127.0.0.1:8000",

  // Development-only fallback. Leave false for normal operation — see
  // js/dev-fallback-data.js for what this does and why it stays isolated.
  USE_DEV_FALLBACK: false
};
